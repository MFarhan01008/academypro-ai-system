import Link from 'next/link';
import { getCurrentUserAndAcademy } from '@/lib/admin-data';
import { formatPKR, shortId } from '@/lib/utils';
export default async function TeacherDetail({params}:{params:Promise<{id:string}>}){const {id}=await params;const {supabase,academy}=await getCurrentUserAndAcademy();if(!academy)return <p>Missing academy</p>;const {data:t}=await supabase.from('teachers').select('*').eq('id',id).eq('academy_id',academy.id).single();if(!t)return <p>Teacher not found</p>;const [stRes,schRes,salRes,feeRes]=await Promise.all([supabase.from('students').select('*').eq('academy_id',academy.id).eq('teacher_id',id),supabase.from('class_schedules').select('*, classes(class_name), subjects(subject_name)').eq('academy_id',academy.id).eq('teacher_id',id),supabase.from('teacher_salaries').select('*').eq('academy_id',academy.id).eq('teacher_id',id).order('salary_month',{ascending:false}),supabase.from('fees').select('*').eq('academy_id',academy.id).eq('teacher_id',id)]);const students=stRes.data||[], schedules=schRes.data||[], salaries=salRes.data||[], fees=feeRes.data||[];const collected=fees.reduce((s:number,f:any)=>s+Number(f.paid_amount||0),0);return <div className="space-y-6"><div className="flex justify-between"><div><h1 className="text-2xl font-black">{t.full_name}</h1><p className="text-sm font-bold text-blue-700">{t.teacher_code||shortId('TCH',t.id)} • {t.subject_specialty||'Teacher'}</p></div><div className="flex gap-2"><Link href={`/admin/teachers/${id}/edit`} className="rounded-2xl border px-4 py-2 text-sm font-bold">Edit</Link><Link href={`/admin/teachers/${id}/salary`} className="rounded-2xl bg-blue-600 px-4 py-2 text-sm font-bold text-white">Add Salary</Link></div></div><div className="grid gap-4 md:grid-cols-4"><Card t="Students" v={String(students.length)}/><Card t="Classes" v={String(schedules.length)}/><Card t="Fee Collected" v={formatPKR(collected)}/><Card t="Salary" v={formatPKR(t.monthly_salary)}/></div><div className="grid gap-6 lg:grid-cols-2"><section className="rounded-2xl border bg-white p-6"><h2 className="font-black">Private Admin Details</h2><div className="mt-4 space-y-2 text-sm"><p><b>Phone:</b> {t.phone||'-'}</p><p><b>WhatsApp:</b> {t.whatsapp||'-'}</p><p><b>CNIC:</b> {t.cnic||'-'}</p><p><b>Address:</b> {t.address||'-'}</p><p><b>Qualification:</b> {t.qualification||'-'}</p><p><b>Experience:</b> {t.experience_years||0} years</p><p><b>Bio:</b> {t.bio||'-'}</p></div></section><section className="rounded-2xl border bg-white p-6"><h2 className="font-black">Assigned Classes</h2><div className="mt-4 divide-y rounded-xl border">{schedules.length?schedules.map((s:any)=><div key={s.id} className="p-4 text-sm"><b>{s.classes?.class_name||'-'}</b> • {s.subjects?.subject_name||'-'} • {s.start_time}-{s.end_time}</div>):<p className="p-4 text-sm text-slate-500">No schedules.</p>}</div></section><section className="rounded-2xl border bg-white p-6"><h2 className="font-black">Students in Teacher Classes</h2><div className="mt-4 divide-y rounded-xl border">{students.length?students.map((s:any)=><div key={s.id} className="p-4 text-sm"><b className="text-blue-700">{s.student_code||shortId('STU',s.id)}</b> • {s.full_name} • {formatPKR(s.monthly_fee)}</div>):<p className="p-4 text-sm text-slate-500">No students assigned.</p>}</div></section><section className="rounded-2xl border bg-white p-6"><h2 className="font-black">Salary History</h2><div className="mt-4 divide-y rounded-xl border">{salaries.length?salaries.map((s:any)=><div key={s.id} className="p-4 text-sm">{s.salary_month} • Base {formatPKR(s.base_salary)} • Paid {formatPKR(s.paid_amount)} • {s.status}</div>):<p className="p-4 text-sm text-slate-500">No salary records.</p>}</div></section></div></div>}
function Card({t,v}:{t:string;v:string}){return <div className="rounded-2xl border bg-white p-5"><p className="text-sm text-slate-500">{t}</p><p className="mt-2 text-2xl font-black">{v}</p></div>}
