import Link from 'next/link';
import { getCurrentUserAndAcademy } from '@/lib/admin-data';
import { formatPKR } from '@/lib/utils';
export default async function Schedules(){const {supabase,academy}=await getCurrentUserAndAcademy();if(!academy)return <p>Save settings first.</p>;const {data:schedules=[],error}=await supabase.from('class_schedules').select('*, classes(class_name), subjects(subject_name), teachers(full_name)').eq('academy_id',academy.id).order('start_time');if(error)return <p>Run supabase/teacher-upgrade.sql first.</p>;return <div className="space-y-6"><div className="flex justify-between"><div><h1 className="text-2xl font-black">Class Schedules</h1><p className="text-sm text-slate-600">Class + subject + teacher + timing + batch.</p></div><Link href="/admin/schedules/new" className="rounded-2xl bg-blue-600 px-4 py-2 text-sm font-bold text-white">Add Schedule</Link></div><div className="overflow-hidden rounded-2xl border bg-white"><table className="w-full text-left text-sm"><thead className="bg-slate-50"><tr><th className="px-5 py-4">Class</th><th className="px-5 py-4">Subject</th><th className="px-5 py-4">Teacher</th><th className="px-5 py-4">Batch</th><th className="px-5 py-4">Days</th><th className="px-5 py-4">Timing</th><th className="px-5 py-4">Fee</th></tr></thead><tbody className="divide-y">{schedules.map((s:any)=><tr key={s.id}><td className="px-5 py-4 font-bold">{s.classes?.class_name||'-'}</td><td className="px-5 py-4">{s.subjects?.subject_name||'-'}</td><td className="px-5 py-4">{s.teachers?.full_name||'-'}</td><td className="px-5 py-4">{s.batch_name||'-'}</td><td className="px-5 py-4">{s.days||'-'}</td><td className="px-5 py-4">{s.start_time}-{s.end_time}</td><td className="px-5 py-4">{formatPKR(s.monthly_fee)}</td></tr>)}</tbody></table></div></div>}
